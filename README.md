# 🔐 强密码生成器 | Password Generator

<p align="center">
  <img src="https://img.shields.io/badge/version-2.0.0-blue.svg" alt="Version">
  <img src="https://img.shields.io/badge/license-MIT-green.svg" alt="License">
  <img src="https://img.shields.io/badge/platform-Web%20%7C%20Windows%20%7C%20macOS%20%7C%20Linux-brightgreen.svg" alt="Platform">
</p>

<p align="center">
  <b>🌐 语言 / Languages:</b><br>
  <a href="README.md">简体中文</a> |
  <a href="README_EN.md">English</a> |
  <a href="README_JA.md">日本語</a> |
  <a href="README_AR.md">العربية</a>
</p>

<p align="center">
  一个功能强大、安全可靠的密码生成工具<br>
  支持 Web、Windows、macOS、Linux 全平台
</p>

---

## 🌐 在线使用

**无需下载，打开浏览器即可使用：**

### 👉 https://zhuyikai2002.github.io/password-generator/

---

## ✨ 功能特性

| 功能 | 描述 |
|------|------|
| 🔢 **可配置长度** | 支持 8-128 位密码长度自由调整 |
| 📊 **强度评估** | 实时计算密码熵值，估算暴力破解所需时间 |
| 🎯 **字符类型** | 可选择大写字母、小写字母、数字、特殊字符 |
| 🚫 **排除混淆** | 可排除易混淆字符 (0O1lI\|) |
| 📋 **一键复制** | 点击即可复制到剪贴板 |
| 📁 **多种格式** | 支持交互式、JSON、纯文本输出 |
| 🔒 **安全随机** | 使用密码学安全的随机数生成器 |
| 📜 **历史记录** | 本地保存生成历史（Web 版） |

---

## 📦 下载安装

### 方式一：在线使用（推荐）

直接访问 https://zhuyikai2002.github.io/password-generator/

### 方式二：下载客户端

前往 [Releases 页面](https://github.com/zhuyikai2002/password-generator/releases) 下载：

| 文件 | 平台 | 大小 | 说明 |
|------|------|------|------|
| `pwgen-win.exe` | Windows | ~36MB | 🟢 绿色免安装，双击运行 |
| `PasswordGenerator-2.0.0-macOS.dmg` | macOS | ~334KB | 应用程序（带图标） |
| `pwgen` | macOS | ~103KB | 命令行工具 (Swift) |
| `pwgen-linux` | Linux | ~44MB | 命令行工具 |

---

## 🚀 快速开始

### Windows 用户

1. 下载 `pwgen-win.exe`
2. 双击运行即可，无需安装
3. 如被安全软件拦截，点击"更多信息" → "仍要运行"

### macOS 用户

**图形界面：**
1. 下载 `PasswordGenerator-2.0.0-macOS.dmg`
2. 打开 DMG，将应用拖拽到 Applications
3. 双击运行

**命令行：**
```bash
# 下载并添加执行权限
chmod +x pwgen

# 运行
./pwgen

# 全局安装（可选）
sudo cp pwgen /usr/local/bin/
```

### Linux 用户

```bash
# 下载并添加执行权限
chmod +x pwgen-linux

# 运行
./pwgen-linux
```

---

## 📖 命令行使用指南

### 基本用法

```bash
# 交互模式（推荐新手）
pwgen

# 生成指定长度的密码
pwgen -l 16

# 批量生成多个密码
pwgen -l 20 -c 5 -b

# 排除易混淆字符
pwgen -l 16 -e
```

### 所有参数

| 参数 | 完整形式 | 说明 | 默认值 |
|------|----------|------|--------|
| `-l` | `--length` | 密码长度 | 12 |
| `-c` | `--count` | 生成数量 | 3 |
| `-e` | `--exclude` | 排除混淆字符 | 否 |
| `-b` | `--batch` | 批量模式 | 否 |
| | `--no-upper` | 不含大写字母 | 否 |
| | `--no-lower` | 不含小写字母 | 否 |
| | `--no-digits` | 不含数字 | 否 |
| | `--no-special` | 不含特殊字符 | 否 |
| | `--json` | JSON 格式输出 | 否 |
| | `--plain` | 纯文本输出 | 否 |
| `-h` | `--help` | 显示帮助 | - |

### 输出格式示例

**交互模式：**
```
╔══════════════════════════════════════════════════════════╗
║                   强密码生成器 v2.0                       ║
╚══════════════════════════════════════════════════════════╝

生成的密码：

  [1] Kx9#mPq$Lw2@
      🟣 强度: 极强 | 熵值: 78.66 bits | 破解时间: 百万年

命令: [1-3] 选择 | [r] 重新生成 | [l] 改长度 | [q] 退出
```

**JSON 格式：**
```json
{
  "generated_at": "2024-01-29T12:00:00Z",
  "passwords": [
    {
      "password": "Kx9#mPq$Lw2@",
      "entropy": 78.66,
      "strength": "极强"
    }
  ]
}
```

---

## 🔒 密码强度说明

| 等级 | 图标 | 熵值 (bits) | 破解时间估算 |
|------|------|-------------|--------------|
| 极弱 | 🔴 | < 28 | 瞬间 |
| 弱 | 🟠 | 28-36 | 数小时 |
| 中等 | 🟡 | 36-60 | 数天至数月 |
| 强 | 🟢 | 60-80 | 数年至数百年 |
| 很强 | 🔵 | 80-100 | 数千年 |
| 极强 | 🟣 | > 100 | 宇宙年龄级别 |

> 💡 **建议：** 重要账户请使用 16 位以上、熵值 80+ 的密码

---

## 🏗️ 技术架构

本项目采用多语言实现，适应不同平台和场景：

```
password-generator/
├── docs/                    # Web 版本 (HTML/CSS/JS)
│   └── index.html
├── generate-password.py     # Python 版本 (功能最全)
├── generate-password.js     # Node.js 版本 (跨平台打包)
├── generate-password.swift  # Swift 版本 (macOS 原生)
├── pwgen                    # Swift 编译二进制
└── dist/                    # 打包输出
    ├── pwgen-win.exe        # Windows 可执行文件
    ├── pwgen-linux          # Linux 可执行文件
    └── pwgen-node           # Node.js 打包版本
```

### 安全性说明

- **Web 版本**: 使用 `crypto.getRandomValues()` Web Crypto API
- **Python 版本**: 使用 `secrets` 模块（密码学安全）
- **Swift 版本**: 使用系统级安全随机数
- **Node.js 版本**: 使用 `crypto.randomInt()`

所有版本均使用密码学安全的随机数生成器，确保生成的密码具有足够的随机性。

---

## 🛠️ 本地开发

### 克隆项目

```bash
git clone https://github.com/zhuyikai2002/password-generator.git
cd password-generator
```

### 运行各版本

```bash
# Python 版本
python3 generate-password.py

# Node.js 版本
node generate-password.js

# Swift 版本（需要 macOS）
swift generate-password.swift
# 或编译后运行
swiftc -O -o pwgen generate-password.swift
./pwgen
```

### 打包构建

```bash
# 打包 Windows
npx pkg generate-password.js -t node18-win-x64 -o dist/pwgen-win.exe

# 打包 Linux
npx pkg generate-password.js -t node18-linux-x64 -o dist/pwgen-linux

# 打包 macOS (Node.js)
npx pkg generate-password.js -t node18-macos-arm64 -o dist/pwgen-node

# 编译 Swift (macOS)
swiftc -O -o pwgen generate-password.swift
```

---

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'Add amazing feature'`)
4. 推送分支 (`git push origin feature/amazing-feature`)
5. 创建 Pull Request

---

## 📄 开源协议

本项目采用 [MIT 协议](LICENSE) 开源。

---

## 🙏 致谢

- 感谢所有贡献者
- 图标设计灵感来自现代 UI 设计趋势

---

<p align="center">
  如果这个项目对你有帮助，请给个 ⭐ Star 支持一下！
</p>
