# 强密码生成器 v2.0

一个功能丰富的命令行密码生成工具，支持多种语言实现和跨平台打包。

## 功能特性

- 🔐 **可配置密码长度** (8-128位)
- 📊 **密码强度评估** (熵值计算 + 破解时间估算)
- 🚫 **排除易混淆字符** (0O1lI|)
- 📋 **自动复制到剪贴板**
- 📁 **多种输出格式** (交互式 / JSON / 纯文本)
- 📜 **密码历史记录** (仅 Python 版本)

## 可执行文件

| 文件 | 语言 | 大小 | 说明 |
|------|------|------|------|
| `pwgen` | Swift | 103KB | macOS 原生二进制，无需依赖 |
| `dist/pwgen-node` | Node.js | 44MB | Node.js 打包，跨平台 |

## 使用方法

### 直接运行

```bash
# Swift 版本 (推荐，最轻量)
./pwgen

# Node.js 打包版本
./dist/pwgen-node

# Python 版本
python3 generate-password.py

# Node.js 脚本版本
node generate-password.js
```

### 命令行参数

```bash
# 生成16位密码
./pwgen -l 16

# 生成5个20位密码（批量模式）
./pwgen -l 20 -c 5 -b

# 排除易混淆字符
./pwgen -l 16 -e

# JSON 格式输出
./pwgen -l 16 --json

# 纯文本输出（适合脚本调用）
./pwgen -l 16 --plain

# 查看帮助
./pwgen --help
```

### 参数说明

| 参数 | 说明 |
|------|------|
| `-l, --length <N>` | 密码长度 (默认: 12, 范围: 8-128) |
| `-c, --count <N>` | 生成数量 (默认: 3) |
| `-e, --exclude` | 排除易混淆字符 (0O1lI\|) |
| `--no-upper` | 不包含大写字母 |
| `--no-lower` | 不包含小写字母 |
| `--no-digits` | 不包含数字 |
| `--no-special` | 不包含特殊字符 |
| `--plain` | 纯文本输出 |
| `--json` | JSON 格式输出 |
| `-b, --batch` | 批量模式（非交互） |
| `-h, --help` | 显示帮助 |

## 密码强度等级

| 等级 | 图标 | 熵值 (bits) | 说明 |
|------|------|-------------|------|
| 极弱 | 🔴 | < 28 | 容易被暴力破解 |
| 弱 | 🟠 | 28-36 | 可能在数小时内被破解 |
| 中等 | 🟡 | 36-60 | 可抵御一般攻击 |
| 强 | 🟢 | 60-80 | 可抵御大多数攻击 |
| 很强 | 🔵 | 80-100 | 非常安全 |
| 极强 | 🟣 | > 100 | 几乎不可能被破解 |

## 全局安装

```bash
# 将 Swift 版本复制到系统路径
sudo cp pwgen /usr/local/bin/

# 之后可以在任何地方使用
pwgen -l 16
```

## 打包命令

### Swift 重新编译
```bash
swiftc -O -o pwgen generate-password.swift
```

### Node.js 跨平台打包
```bash
# macOS ARM64
npx pkg generate-password.js -t node18-macos-arm64 -o dist/pwgen-macos-arm64

# macOS x64
npx pkg generate-password.js -t node18-macos-x64 -o dist/pwgen-macos-x64

# Linux x64
npx pkg generate-password.js -t node18-linux-x64 -o dist/pwgen-linux-x64

# Windows x64
npx pkg generate-password.js -t node18-win-x64 -o dist/pwgen-win-x64.exe
```

## 目录结构

```
password-generator/
├── pwgen                    # Swift 编译后的二进制
├── generate-password.py     # Python 版本 (功能最全)
├── generate-password.js     # Node.js 版本
├── generate-password.swift  # Swift 源码
├── package.json             # Node.js 项目配置
├── dist/
│   └── pwgen-node           # Node.js 打包后的二进制
└── README.md
```

## License

MIT
